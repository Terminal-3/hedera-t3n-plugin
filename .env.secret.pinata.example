# Pinata credentials for `pnpm ipfs-submit-agent-card-pinata`
# Provide either:
# 1) PINATA_JWT
# or
# 2) PINATA_API_KEY + PINATA_API_SECRET

PINATA_JWT=""

# Optional fallback if JWT is not available:
PINATA_API_KEY=""
PINATA_API_SECRET=""
